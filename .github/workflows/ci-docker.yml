# Generated automatically by org-governance-bot. Do not edit manually.
name: "Docker CI"

on:
  pull_request:
    branches:
      - main
      - release/**
    paths:
      - Dockerfile
      - bin/**
      - config/**
      - .github/workflows/ci-docker.yml

jobs:
  orchestrate:
    uses: blinklabs-io/actions/.github/workflows/reuseable-ci-docker-multiarch.yml@main
    with:
      build-target: cardano-node
      image-name: blinklabs-io/cardano-node
